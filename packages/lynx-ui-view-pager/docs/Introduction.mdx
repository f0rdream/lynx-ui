import { Go } from "@lynx/index";

## Basic Usage

Give the pager and every child page explicit dimensions.

<Go
  example="ViewPager"
  defaultEntryName="ViewPagerBasic"
  defaultFile="Basic/index.tsx"
  entry="Basic"
/>

## Imperative Selection

Use a ref and `selectTab(index, smooth)` to move to a page programmatically.
