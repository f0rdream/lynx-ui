import { Go } from "@lynx/index";

## 基础用法

为分页容器和每个子页面设置明确的尺寸。

<Go
  example="ViewPager"
  defaultEntryName="ViewPagerBasic"
  defaultFile="Basic/index.tsx"
  entry="Basic"
/>

## 主动切换

通过 ref 调用 `selectTab(index, smooth)` 主动切换页面。
